plugins { id "com.android.application" }

android {
    namespace "fr.stephane.suivientrainement"
    compileSdk 35

    defaultConfig {
        applicationId "fr.stephane.suivientrainement"
        minSdk 26
        targetSdk 35
        versionCode 45
        versionName "4.5"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }
}
